/*链接：https://ac.nowcoder.com/acm/contest/117136/1040
来源：牛客网

小w想和你van纸牌
小w有两张纸牌，两张纸牌上都有相同的正整数n
每一轮一张纸牌上的数都可以减去小于等于另外一张纸牌上的数的数
每一轮只能操作和上轮不同的纸牌
小w想知道三轮之后两纸牌上数字之和的最小值
注意，不能减为负数
输入描述:

第一行1个正整数n。

输出描述:

一行一个整数
表示三轮之后两纸牌上数字和的最小值*/

#include <stdio.h>

int main()
{
    int n;

    // 提示用户输入正整数n
    scanf("%d", &n);

    // 检查输入是否有效
    if (n <= 0)
    {
        printf("n必须是正整数。\n");
        return 1;
    }

    // 计算三轮后两牌上数字之和的最小值
    int min_sum = n - (n / 2);

    // 输出结果
    printf("%d\n", min_sum);

    return 0;
}